export default class FeedController {

    // Takes variable number of `Feed` objects
    // Return a single array of sorted articles
    sort(...feeds) {
        // For feed in feeds
            // For article in feeds
                // Format and add to combined article list
        // Sort article list
        // Return articles
    }
}